import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { Observable, Subscription } from 'rxjs';
import { Option, Selection } from './interfaces';
export declare class OptionsComponent implements OnInit, OnDestroy {
    allowNewOptions: boolean;
    options: Option[];
    selections: Selection[];
    showOptions: boolean;
    inputValue: string;
    keyup: Observable<KeyboardEvent>;
    emptyOptions: string;
    onSelect: EventEmitter<string>;
    activeOptionIndex: number;
    filteredOptions: Option[];
    keySubscription: Subscription;
    select(event: MouseEvent, option: Option): void;
    ngOnChanges(changes: any): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    filterOptions(options: Option[], selections: Selection[], input?: string): Option[];
}
